# Changelog

All notable changes to `app-dev-os` and the projects built with it are logged here. Each entry is a real change, not a placeholder — this file doubles as a running content backlog for posts.

## [Unreleased]

- Repo genericized from `divya-dev-os`: reframed as a reusable stage-gated Claude Code system, with ContractIQ kept in as a worked example rather than the primary framing.
